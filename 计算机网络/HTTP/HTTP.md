# HTTP

> HTTP(HyperText Transfer Protocol 超文本传输协议) 是基于TCP的 应用层协议
  
  - HTTP 用于客户端和服务端之间的通信；在一条通信链路上必然有一端是客户端，另一端是服务端

  - HTTP 通过请求和响应的交换达成通信；请求从客户端发出，服务端响应请求并返回

  - HTTP 是无状态的协议，自身不对请求和响应之间的通信状态进行保存；但是可以通过`Cookie`技术实现对状态的保持

  - HTTP 通过URI定位互联网上的资源